# ============================================================
# ShadowNet Defender Extractor V2 — requirements.txt
# Perfil completo: instala todo lo necesario para el proyecto.
# ============================================================
#
# Perfil por defecto (producción + desarrollo + viz + ML):
-r requirements/base.in
-r requirements/ml.in
-r requirements/viz.in
-r requirements/dev.in

# Perfiles individuales (uso alternativo):
# pip install -r requirements/base.in       → solo producción / inferencia
# pip install -r requirements/ml.in         → + entrenamiento / evaluación
# pip install -r requirements/viz.in        → + gráficos / análisis visual
# pip install -r requirements/dev.in        → + API, servidor y testing
