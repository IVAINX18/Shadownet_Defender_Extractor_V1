# Default reproducible install profile (runtime only)
-r requirements/base.lock.txt

# Optional profiles:
# pip install -r requirements/ml.lock.txt
# pip install -r requirements/viz.lock.txt
# pip install -r requirements/dev.lock.txt
