# Copy this file to .env.render-sync and fill the values.

# Render API token with permission to edit service env vars.
RENDER_API_KEY=your_render_api_key

# Render service id (not service name).
# Example: srv-xxxxxxxxxxxxxxxxxxxx
RENDER_SERVICE_ID=your_render_service_id

# Optional: Render API base URL.
RENDER_API_BASE=https://api.render.com/v1

# Static env file to sync before tunnel starts.
SYNC_STATIC_ENV_FILE=.env.render-values

# File generated by the script with the latest tunnel URL + /v1.
RUNTIME_ENV_FILE=.env.tunnel-runtime

# Trigger a deploy after each env update: true|false
RENDER_TRIGGER_DEPLOY=true

# cloudflared quick tunnel target and host header for Ollama.
TUNNEL_TARGET=http://localhost:11434
TUNNEL_HOST_HEADER=localhost:11434

# Restart delay when cloudflared exits unexpectedly.
RESTART_DELAY_SECONDS=3
